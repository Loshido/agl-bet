services:
  db:
    env_file:
      - .env
    ports:
      - 5432:5432
    image: postgres:18-alpine
    container_name: agl-db
    volumes:
      - agl-db:/var/lib/postgresql/data
    restart: always

volumes:
  agl-db:
    name: agl-vol