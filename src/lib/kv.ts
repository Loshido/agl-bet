const kv = new Map<string, string>()

kv.set('inscriptions', '1') // '0' | '1' (active ou desactive les inscriptions)
kv.set('ballons', '0') // '0' | '1' (active des ballons/confettis sur la page du classement)

export default kv;